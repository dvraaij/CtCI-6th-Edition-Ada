with "aunit";

project CtCI is

   for Source_Dirs use ("src", "src/chapter_1", "src/chapter_2");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("main.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-g");
   end Compiler;

end CtCI;

